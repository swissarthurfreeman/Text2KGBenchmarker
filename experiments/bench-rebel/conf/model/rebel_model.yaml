# @package _global_

# folder containing all Rebel-large files, weights, embeddings etc.
# by default, we use the same tokenizer as REBEL, e.g. as BART
pretrained_model_name_or_path: 'experiments/bench-rebel/Rebel-large'
tokenizer_name_or_path: 'experiments/bench-rebel/Rebel-large'

